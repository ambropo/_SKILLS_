---
name: compile-latex
description: Compile LaTeX documents using the xelatex → bibtex → xelatex → xelatex pipeline. Use when user asks to compile, build, or typeset a LaTeX file.
argument-hint: "[path-to-tex-file]"
allowed-tools: ["Bash", "Read", "Glob"]
---
# Compile LaTeX
Run: xelatex → bibtex → xelatex → xelatex
